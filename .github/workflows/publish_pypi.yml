---
name: publish-pypi

# yamllint disable-line rule:truthy
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to publish (branch or tag, e.g. v0.1.1). Empty = branch selected in the UI."
        required: false
        type: string
      skip_publish:
        description: "Run tests only (do not upload to PyPI)"
        required: false
        type: boolean
        default: false

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.ref }}
      - name: Setup CI
        uses: ./.github/actions/setup-ci
        with:
          python_version: ${{ matrix.python-version }}
          uv_args: "--all-groups"
      - name: Run full test gate
        run: task test

  publish:
    needs: test
    if: ${{ !inputs.skip_publish }}
    runs-on: ubuntu-latest
    # Job-level id-token is required for OIDC trusted publishing
    permissions:
      contents: read
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/p/mrjk.varstore
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.ref }}
      - name: Setup CI
        uses: ./.github/actions/setup-ci
        with:
          python_version: "3.12"
          uv_args: "--group dev"
      - name: Publish to PyPI
        run: |
          uv build
          uv publish --trusted-publishing always
